import { CompanyFormatPipe } from './company-format.pipe';

describe('CompanyFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CompanyFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
